apiVersion: v1
kind: Service
metadata:
  name: {{ include "camel-dashboard-all.fullname" . }}
  labels:
    {{- include "camel-dashboard-all.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "camel-dashboard-all.selectorLabels" . | nindent 4 }}
